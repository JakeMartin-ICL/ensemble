services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    env_file: .env
    # For production on Unraid, run this service directly.
    # Supabase is managed externally - no local db container needed.
